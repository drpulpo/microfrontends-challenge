import React from "react";
import ReactDOM from "react-dom";

import {StyledFooter} from './styles'

export const Footer= ()=>{
    return (
        <StyledFooter>
            <span className="footer__text">David Nuñez 2023</span>
        </StyledFooter>
        );
}